This webpage requires live hosting (Like through the Live Server extension on VSCode) to have all of it's features work perfectly, especially the database

If the webpages passwords.html and emails.html are just opened as HTML files locally, then the Javscript will fail to fetch the database JSON and the search function willnot work
